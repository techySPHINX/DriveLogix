import { AppRegistry } from "react-native";
import App from "./App";
const appName = "Dynamic";

// Register the main app component (App.tsx) with the app name
AppRegistry.registerComponent(appName, () => App);
